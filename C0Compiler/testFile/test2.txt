int a,b;
int isPositive(){
	if(a/(a+1))
		return(0);
	else
		return(1);
}
int isEqual(){
 	int c ;
 	c = a - b;
 	if(c) return(0);
 	else return(1);
}
int isGreater(){
	int c ;
	if(isEqual())
		return (0);
	c = a - b;
	a = c;
	b = -1;
    if(isEqual())
        return (0);
	if(isPositive())
		return (1);
	else return (0);
}
void main()
{
    scanf(a);
    scanf(b);
    if(isGreater()){
        printf(1);
    }else{
        printf(0);
    }
}